import React from 'react'
import { connect } from 'react-redux'

// this will be a function to import a map if the farm you want to upload. You'll also need to enter settings such as name, size and other information
// this information can then be stored in the server backend

export class Import extends React.Component {

}


export default connect(mapStateToProps)(Import)